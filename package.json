{
  "name": "cavia-avonturen-wereld",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "serve": "npx serve .",
    "lint": "npx eslint js --ext .js",
    "format": "npx prettier --write ."
  },
  "devDependencies": {}
}